1. 在stu_generate_matching里面，点击对局信息要保证可以弹出新的标签页（这个新的标签页的内容是spectator_display.html），注意还是停留在当前页面上，/originals弹出页的信息：在stu_generate_matching页面里，读取参赛者的姓名、道场（学校）、组别信息。
   并且将读取到的信息展示在spectator的页面上，当裁判在标签页点击开始按钮时，spectator同步开始倒计时。我是要这两个页面到时候分成两个屏幕同时放在两台设备上的

2. 当裁判员点击了弹出标签页的暂停、恢复按钮时，把按钮的颜色标记为已点击的状态（比如标记为灰色）（可重复点击，不能变色了以后就不能点击了），对应spectator必须做出相应动作（点击暂停按钮，spectator暂停倒计时；点击恢复按钮，spectator恢复倒计时）

3. 在裁判员点击某行弹出的控制面板上提示：倒计时（与spectator的倒计时同步显示）。


prompt -> trae -> 做修改 -> 人工微调一下。