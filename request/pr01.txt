1. 在stu_generate_matching里面，点击了开始按钮要保证可以弹出新的标签页。
   标签页的信息：在stu_generate_matching页面里，读取参赛者的姓名、道场信息。
   并且将读取到的信息展示在spectator的页面上，并且开始倒计时。

2. 当裁判员点击了暂停按钮时，最好把按钮的颜色标记为以点击的状态（可重复点击，不能变色了以后就不能点击不了了）
3. 在裁判员点击某行弹出的控制面板上提示：倒计时。


prompt -> trae -> 做修改 -> 人工微调一下。